include $(HOME)/Makefile.ini

CLEANFILES=*.a *.list *.o
OBJS=bpbdplmt01.o
BIN_TARGET=$(DIR_BIN)/bpbdplmt01

all:$(BIN_TARGET)

$(BIN_TARGET):$(OBJS)
	$(GCC) $? -o $@ $(LFLAG)
	@echo "##### $@ make success #####"

clean:
	rm -f $(CLEANFILES)
