include $(HOME)/Makefile.ini

CLEANFILES=*.a *.list *.o

BIN_TARGET=$(DIR_BIN)/bpcccoco01

all:$(BIN_TARGET)

$(BIN_TARGET):$(OBJ_C) $(OBJ_PC)
	$(GCC) $? -o $@ $(LFLAG)
	@echo "##### $@ make success #####"

clean:
	rm -f $(CLEANFILES)
